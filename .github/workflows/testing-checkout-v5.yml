name: testing checkout v5

on: 
  workflow_dispatch:
  push:
  pull_request:

jobs:
  test-checkout-v5:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-22.04, windows-latest, macos-latest, macos-13, macos-14]
    steps:
      - name: Get Node.js version
        run: node --version

      - name: Checkout code
        uses: actions/checkout@v5

      - name: Get Node.js version again
        run: node --version